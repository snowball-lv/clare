

# generate source files
if ARGV.include? "clean"
    sh "rake -f rakefile-gen.rb clean"
else
    sh "rake -f rakefile-gen.rb"
end

# run the build already with the generated source
require_relative "rakefile-main"
