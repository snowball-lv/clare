

RULE(IR.Mov(IR.Tmp(), IR.Exp()), {
    Munch(root->right);
    printf("store\n");
})

RULE(IR.Add(IR.Exp(), IR.Exp()), {
    Munch(root->left);
    Munch(root->right);
    printf("add\n");
})

RULE(IR.I32(0), {
    printf("push i32\n");
})
