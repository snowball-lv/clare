

RULE(IR.Mov(IR.Tmp(), 0), {
    Munch(root->right);
    printf("store\n");
})

RULE(IR.Add(0, 0), {
    Munch(root->left);
    Munch(root->right);
    printf("add\n");
})

RULE(IR.I32(0), {
    printf("push i32\n");
})
