

func clare_main

    $sum = i32(0)
    
    $i = i32(0)
    while lt($i, i32(10)) do
        
        $k = i32(0)
        while lt($k, i32(10)) do
            
            $n = i32(0)
            while lt($n, i32(10)) do
                
                $sum = add($sum, $n)
                
                $n = add($n, i32(1))
            end
            
            $k = add($k, i32(1))
        end
        
        $i = add($i, i32(1))
    end
    
    ret($sum)
end
